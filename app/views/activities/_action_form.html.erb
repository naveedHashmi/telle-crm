
  <%= form_with(url: status_action_path(activity), class: 'activity-action-form', data: {action: 'submit->activities#submit_activities'}) do |form| %>
    <%= form.submit status_action_text(activity), class: status_action_class(activity) %>
  <% end %>
